name: Build and Release Jar

on:
  push:
    branches:
      - main

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: List target contents
        run: ls -R target

      - name: Find and Rename Jar
        run: |
          mkdir -p release
          JAR=$(find target -maxdepth 1 -type f -name "mc-watchdog-*.jar" ! -name "original-*.jar" | head -n 1)
          echo "✅ Found jar: $JAR"
          cp "$JAR" "release/mc-watchdog-${{ github.run_id }}.jar"

      - name: Release to GitHub
        uses: softprops/action-gh-release@v1
        with:
          tag_name: release-${{ github.run_id }}
          name: Auto Release ${{ github.run_id }}
          files: release/mc-watchdog-${{ github.run_id }}.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GH_RELEASE_TOKEN }}
